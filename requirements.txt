# Core dependencies
numpy>=1.21.0
sympy>=1.10.0
matplotlib>=3.5.0

# LangChain and OpenAI
langchain>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5
langchain-core>=0.1.0
openai>=1.0.0
python-dotenv>=0.19.0

# Data processing and visualization
pandas>=1.3.0
tabulate>=0.9.0

# Animation and visualization support
# NOTE: Also requires ffmpeg system binary to be installed
# Install via: conda install ffmpeg -y
# Or on Linux: sudo apt install ffmpeg
# Or on macOS: brew install ffmpeg
imageio>=2.9.0
imageio-ffmpeg>=0.4.0
cairosvg>=2.5.0

